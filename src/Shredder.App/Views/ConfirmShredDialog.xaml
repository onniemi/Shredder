<ui:FluentWindow x:Class="Shredder.App.Views.ConfirmShredDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="确认粉碎" Height="460" Width="560"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="确认粉碎"/>

        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ui:InfoBar Grid.Row="0" Severity="Error" IsOpen="True" IsClosable="False"
                        Title="粉碎操作不可恢复"
                        Message="安全擦除后无法用任何工具找回,请再次确认。"/>

            <ui:TextBlock Grid.Row="1" Margin="0,12,0,8" FontTypography="Body" TextWrapping="Wrap"
                          Text="以下文件 / 目录将被安全擦除:"/>

            <Border Grid.Row="2"
                    BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="6">
                <ListBox x:Name="PathList" BorderThickness="0" Background="Transparent">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,4">
                <ui:TextBlock VerticalAlignment="Center" FontTypography="Body"
                              Text="在下方输入框中精确输入「"/>
                <ui:TextBlock VerticalAlignment="Center" FontTypography="BodyStrong"
                              Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                              Text="{Binding RequiredKeyword}"/>
                <ui:TextBlock VerticalAlignment="Center" FontTypography="Body"
                              Text="」并等待倒计时结束:"/>
            </StackPanel>

            <ui:TextBox Grid.Row="4" x:Name="KeywordBox" PlaceholderText="输入确认关键词..."
                        TextChanged="OnKeywordChanged"/>

            <Grid Grid.Row="5" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ui:TextBlock VerticalAlignment="Center" Appearance="Tertiary" FontTypography="Body"
                              Text="{Binding HintText}"/>
                <ui:Button Grid.Column="1" Content="取消" MinWidth="80" Margin="0,0,8,0"
                           IsCancel="True" Click="OnCancelClick"/>
                <ui:Button Grid.Column="2" x:Name="ConfirmButton" Content="我确认,开始粉碎"
                           Appearance="Danger" MinWidth="160" IsEnabled="False"
                           Icon="{ui:SymbolIcon DeleteDismiss24}"
                           Click="OnConfirmClick"/>
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>
